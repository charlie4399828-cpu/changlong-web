{
  "name": "changlong-web",
  "version": "1.0.0",
  "private": true,
  "description": "昌隆茶业官方网站",
  "scripts": {
    "start": "npx serve . -l 8080",
    "deploy": "npx vercel --prod",
    "deploy:vercel": "npx vercel --prod",
    "deploy:netlify": "npx netlify deploy --prod --dir=."
  },
  "devDependencies": {
    "@supabase/supabase-js": "^2.49.8"
  }
}
